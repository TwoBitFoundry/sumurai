#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run frontend checks
cd frontend

echo "🔍 Running Biome on staged files..."
pnpm check --staged

# Run backend checks
cd ../backend

echo "🔍 Running Clippy linting on backend..."
cargo clippy --all-targets --all-features

echo "✅ Pre-commit checks passed!"
